pipeline {
    agent {
        docker {
            // Replace with the image you want to use for your pipeline
            image 'maven:3-jdk-8'
            // Optional: Specify labels for agents
            // label 'my-docker-agent'
        }
    }

    stages {
        stage('Build') {
            steps {
                // Commands to execute for the build stage
                //sh 'mvn clean install'
            }
        }

        stage('Test') {
            steps {
                // Commands to execute for the test stage
                //sh 'mvn test'
            }
        }

        // Add more stages as needed
    }
}
