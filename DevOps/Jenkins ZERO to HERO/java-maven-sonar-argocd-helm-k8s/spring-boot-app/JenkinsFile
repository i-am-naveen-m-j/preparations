pipeline {
    agent {
        docker {
            image 'maven:3-jdk-8'
        }
    }

    stages {
        stage('Checkout') {
            steps {
                // Uncomment the following line when ready to checkout code:
                // git branch: 'master', url: 'https://github.com/your-organization/your-repo.git'
            }
        }

        stage('Build') {
            steps {
                // Uncomment the following line when ready to build:
                // sh 'mvn clean install'
            }
        }

        stage('Test') {
            steps {
                // Uncomment the following line when ready to run tests:
                // sh 'mvn test'
            }
        }

        stage('Deploy') {
            steps {
                // Uncomment the following line when ready to deploy:
                // sh 'scp target/your-artifact.jar your-server:/deploy/path'
            }
        }
    }
}
